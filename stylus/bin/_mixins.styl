iconProperties(start, end, ptop=0, pleft)
    grid-column-start: start
    grid-column-end: end
    padding: ptop 0 0 pleft
    i
        font-size: 24px
        color: light-color
        &:hover
            cursor pointer